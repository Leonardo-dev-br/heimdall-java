CREATE TABLE TB_HDL_MOTO (
    ID NUMBER GENERATED BY DEFAULT ON NULL AS IDENTITY PRIMARY KEY,
    PLATE VARCHAR2(20) NOT NULL,
    CHASSI_NUMBER VARCHAR2(30) NOT NULL,
    MOTORCYCLE_TYPE VARCHAR2(30) NOT NULL
);